name = "argyle-lead-parser"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Secrets (SUPABASE_URL, SUPABASE_SERVICE_KEY) are set via `wrangler secret put`,
# not here. Do not commit secrets.
